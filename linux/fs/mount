# mount device 
mount [option] device mount-point

# options
-a
    mount all fs list in /etc/fstab
-o loop
    specify loop device ???

# unmount
unmount [-f] dir.   # force unmount even it's being used.
